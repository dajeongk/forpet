<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- resources위치 바로 접근 가능(서버 실행) -->
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	
	<!-- 추가주소 -->
	<resources mapping="/bower_components/**" location="/resources/bower_components/" />
	<resources mapping="/dist/**" location="/resources/dist/" />
	<resources mapping="/plugins/**" location="/resources/plugins/" />
	
	<resources mapping="/css/**" location="/resources/css/" />
	<resources mapping="/js/**" location="/resources/js/" />
	
	<resources mapping="/img/**" location="/resources/img/" />
	
	<resources mapping="/ckeditor/**" location="/resources/bower_components/ckeditor/" />
	<resources mapping="/upload/**" location="/resources/upload/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- 프로젝트 생성과함께 자동추가 -->
	<context:component-scan base-package="com.forpet.controller" />
	<!-- 프로젝트 생성과함께 자동추가 -->
	<context:component-scan base-package="com.forpet.exception" />
	
	<!-- 파일업로드 bean설정. id="multipartResolver" 예약어-->
	<beans:bean id="multipartResolver" class="org.springframework.web.multipart.support.StandardServletMultipartResolver"></beans:bean>
	
	<!-- CkEditor 리눅스 경로 : /usr/local/tomcat/tomcat9/webapps -->
	
	<!-- 파일 이미지 업로드 경로 설정. 리눅스 경로 : /usr/local/tomcat -->
	<beans:bean id="uploadPath" class="java.lang.String">
		<beans:constructor-arg value="c:\\dev\\upload\\"></beans:constructor-arg>
	</beans:bean>
	

	<!-- 인터셉터 클래스 bean설정 -->
	<beans:bean id="userLoginInterceptor" class="com.forpet.interceptor.UserLoginInterceptor"></beans:bean>
	<beans:bean id="adminLoginInterceptor" class="com.forpet.interceptor.AdminLoginInterceptor"></beans:bean>
	
	<!-- 인터셉터의 제어를 받을 사용자 매핑주소 설정 -->
	<interceptors>
		<interceptor>
			<!-- 로그인 인증된 경우에만 접근 가능한 주소 -->
			<mapping path="/member/modify"/>
			<mapping path="/user/cart/*"/>
			<mapping path="/user/order/*"/>
			<mapping path="/user/board/*"/>
			
			<beans:ref bean="userLoginInterceptor"/>
		</interceptor>
	</interceptors>
	
	<!-- 인터셉터의 제어를 받을 관리자 매핑주소 설정 -->
	<interceptors>
		<interceptor>
			<mapping path="/admin/**"/>
			<exclude-mapping path="/admin/"/>
			<beans:ref bean="adminLoginInterceptor"/>		
		</interceptor>
	</interceptors> 
	
</beans:beans>
